{
  "name": "mkdsc",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "tauri:dev": "npm --prefix frontend run dev",
    "tauri:build": "npm --prefix frontend run build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
